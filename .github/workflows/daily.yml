name: ASVP Daily Monitor

on:
  workflow_dispatch:
  schedule:
    - cron: "30 4 * * *"

permissions:
  contents: write

concurrency:
  group: asvp-monitor-daily
  cancel-in-progress: false

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Rotate snapshots
        run: |
          mkdir -p data

          if [ -f data/current.json ]; then
            cp data/current.json data/previous.json
            echo "Copied current.json -> previous.json"
          else
            echo "No current.json yet"
          fi

      - name: Run ASVP scan
        run: |
          python scripts/scan_asvp.py

      - name: Build diff
        run: |
          python scripts/diff_snapshots.py

      - name: Commit data and dashboard
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add data/current.json
          git add data/previous.json
          git add data/changes.json
          git add docs/index.html
          git add docs/styles.css
          git add docs/app.js

          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update ASVP dashboard data"
            git push
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: asvp-data
          path: |
            data/current.json
            data/previous.json
            data/changes.json
            docs/index.html
            docs/styles.css
            docs/app.js
